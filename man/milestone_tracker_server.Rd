% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mod_milestone_tracker.R
\name{milestone_tracker_server}
\alias{milestone_tracker_server}
\title{Milestone Tracker — Server}
\usage{
milestone_tracker_server(
  id,
  data,
  row_id,
  group_col,
  group_sort_col = NULL,
  item_col,
  badge_col = NULL,
  badge_colors = NULL,
  annotation_col_specs,
  card1_title = "Select Items",
  card2_title = "Annotations",
  group_label = NULL,
  item_label = NULL,
  count_label = "items"
)
}
\arguments{
\item{id}{`character(1)`. Shiny module namespace ID.}

\item{data}{`reactive`. A reactive returning the source data frame. Must
contain `row_id`, `group_col`, `item_col`, and optionally `badge_col`.}

\item{row_id}{`character(1)`. Name of the unique ID column in `data`
(e.g. `"id"`).}

\item{group_col}{`character(1)`. Column to group rows by in Card 1
(e.g. `"age"`). Clicking a group header expands its rows.}

\item{group_sort_col}{`character(1)` or `NULL`. Column used to sort the
groups (e.g. `"age_months"`). When `NULL`, groups sort by `group_col`.}

\item{item_col}{`character(1)`. Column containing the item text shown in
each row (e.g. `"milestone"`).}

\item{badge_col}{`character(1)` or `NULL`. Column rendered as a coloured
badge in Card 1 (e.g. `"category"`). `NULL` disables badge rendering.}

\item{badge_colors}{Named list or `NULL`. Maps badge values to
`list(bg = "#hex", text = "#hex")` colour specs. Required when
`badge_col` is not `NULL`.}

\item{annotation_col_specs}{`list`. Full col_specs list passed to
[annotator_table_server()] for Card 2. Include display col_specs for
columns you want to show from the selected data, followed by input
col_specs for annotation fields (select, checkbox, text, etc.).}

\item{card1_title}{`character(1)`. Header text for Card 1.}

\item{card2_title}{`character(1)`. Header text for Card 2.}

\item{group_label}{`character(1)` or `NULL`. Display label for the group
column. Defaults to `group_col`.}

\item{item_label}{`character(1)` or `NULL`. Display label for the item
column. Defaults to `item_col`.}

\item{count_label}{`character(1)`. Suffix used in the aggregate row count
(e.g. `"milestones"` → `"5 milestones"`).}
}
\value{
A [shiny::reactive()] returning a data frame of row IDs and
  annotation values for rows where at least one input has been touched.
  Passes through the return value of [annotator_table_server()].
}
\description{
Milestone Tracker — Server
}
